apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: shop
spec:
  selector:
    app: product-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
