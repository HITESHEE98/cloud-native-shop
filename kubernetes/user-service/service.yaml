apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: shop
spec:
  selector:
    app: user-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
